# Local native-development endpoints. Copy this file to `.env.local` before
# running `pnpm tauri dev`; production builds fall back to the HTTPS endpoint.
VITE_VEIL_WS_URL=ws://127.0.0.1:9080/ws
VITE_VEIL_HTTP_URL=http://127.0.0.1:9080
