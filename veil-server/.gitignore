/gateway
/gateway-linux
/veil-gateway

# Release files and their integrity metadata are generated by CI. Keep only
# the operator documentation and the empty directory marker in source control.
cmd/gateway/downloads/*
!cmd/gateway/downloads/.gitkeep
!cmd/gateway/downloads/README.md
