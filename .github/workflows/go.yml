name: Go CI

on:
  push:
    branches: [master, main]
    paths:
      - "veil-server/**"
      - "veil-proto/**"
      - "deploy/**"
      - "scripts/apply-migrations.sh"
      - "scripts/generate-third-party-notices.mjs"
      - "third_party/**"
      - "docker-compose.yml"
      - ".dockerignore"
      - "LICENSE"
      - "NOTICE"
      - "THIRD_PARTY_NOTICES.md"
      - "TRADEMARKS.md"
      - "Caddyfile"
      - ".env.example"
      - ".github/workflows/go.yml"
      - ".github/workflows/server-image.yml"
  pull_request:
    branches: [master, main]
    paths:
      - "veil-server/**"
      - "veil-proto/**"
      - "deploy/**"
      - "scripts/apply-migrations.sh"
      - "scripts/generate-third-party-notices.mjs"
      - "third_party/**"
      - "docker-compose.yml"
      - ".dockerignore"
      - "LICENSE"
      - "NOTICE"
      - "THIRD_PARTY_NOTICES.md"
      - "TRADEMARKS.md"
      - "Caddyfile"
      - ".env.example"
      - ".github/workflows/go.yml"
      - ".github/workflows/server-image.yml"

permissions:
  contents: read

defaults:
  run:
    working-directory: veil-server

jobs:
  configuration:
    name: Container and deployment configuration
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: .
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Validate migration runner syntax
        run: sh -n scripts/apply-migrations.sh

      - name: Validate development Compose
        run: docker compose --env-file .env.example config --quiet

      - name: Validate production Compose
        run: docker compose -f deploy/compose.prod.yml --env-file deploy/.env.example config --quiet

      - name: Build gateway container
        run: docker build --file veil-server/Dockerfile --tag veil-gateway:ci .

  build:
    name: Build & Vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: "1.26.5"
          cache-dependency-path: veil-server/go.sum

      - name: Set up pinned Protocol Buffers compiler
        uses: arduino/setup-protoc@f4d5893b897028ff5739576ea0409746887fa536 # v3.0.0
        with:
          version: "34.1"
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify checked-in Go protocol bindings
        working-directory: .
        run: |
          set -euo pipefail
          go install google.golang.org/protobuf/cmd/protoc-gen-go@v1.36.11
          protoc --proto_path=veil-proto \
            --go_out=. \
            --go_opt=module=github.com/NaveLIL/veil \
            veil-proto/veil/v1/*.proto
          git diff --exit-code -- veil-server/pkg/proto/v1

      - name: Download deps
        run: go mod download

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Install staticcheck
        run: go install honnef.co/go/tools/cmd/staticcheck@v0.6.1

      - name: Staticcheck
        run: staticcheck ./...

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [build, configuration]
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: veil_test
          POSTGRES_USER: veil
          POSTGRES_PASSWORD: veiltest
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U veil"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: "1.26.5"
          cache-dependency-path: veil-server/go.sum

      - name: Run tests
        env:
          DATABASE_URL: postgres://veil:veiltest@localhost:5432/veil_test?sslmode=disable
        run: go test -v -race -timeout 60s ./...

  # End-to-end REST tests against an ephemeral Postgres container spun up
  # via testcontainers-go. Gated by the `integration` build tag so that the
  # default `go test ./...` job above stays Docker-free.
  integration:
    name: Integration (testcontainers)
    runs-on: ubuntu-latest
    needs: [build, configuration]
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version: "1.26.5"
          cache-dependency-path: veil-server/go.sum

      - name: Verify Docker is available
        run: docker info

      - name: Run integration tests
        env:
          # testcontainers-go reuses containers across tests when ryuk is
          # disabled; on CI we want clean teardown.
          TESTCONTAINERS_RYUK_DISABLED: "false"
        run: go test -tags=integration -timeout 5m -v ./internal/integration/...
